

public class Assignment4 {
    public static void main(String[] args) {

        //Don't let the size of this scare you! no matter how big an array is, it all works the same!
        int[] myArray = {10,3,295,38,20,3,4,267,2445,10,5566,87,93,17,10,2,87,267,3176,3,82};
        //you cannot use the array util. Do this one by hand :(

        for(int i = 0; i < myArray.length; i++){
            int count = 1; // Starts at 1 to account for initial number being compared
            for(int j = i+1; j < myArray.length; j++){
                if (myArray[i] == myArray[j]){
                    count++;
                }
            }
            if (count > 1){
                System.out.println(myArray[i] + " appeared " + count + " times.");
            }
        }
  }
}

// I couldn't figure how to prevent dupilicates from being listed multiple times. 
// Thought using a list to track which numbers had already been compared, but it seemed to complicated and I couldn't get it to work.
